{
  "name": "firestore-explorer",
  "version": "1.0.0",
  "description": "Web based admin UI to explore cloud firestore",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "onetime_setup": "cd explorer && yarn && cd ../firebase-proxy && yarn && cp -rf data/sample.db.json data/db.json",
    "serve": "concurrently -k -p \"[{name}]\" -n \"webapp,frproxy\" -c \"yellow.bold,cyan.bold,green.bold\" \"cd explorer && yarn serve\" \"cd firebase-proxy && yarn dev\"",
    "lint": "cd explorer && yarn lint && cd ../firebase-proxy && yarn format"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@gitlab.com/madhu314/firestore-explorer.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://gitlab.com/madhu314/firestore-explorer/issues"
  },
  "homepage": "https://gitlab.com/madhu314/firestore-explorer#readme",
  "dependencies": {
    "concurrently": "^4.1.0"
  },
  "devDependencies": {
    "husky": "^2.7.0"
  }
}
